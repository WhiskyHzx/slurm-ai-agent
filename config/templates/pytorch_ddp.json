{
  "id": "pytorch_ddp",
  "display_name": "PyTorch 多卡训练 (DDP)",
  "description": "适用于大数据集、大模型的多 GPU 分布式训练，使用 torchrun + DDP 策略。",
  "parameters": {
    "job_name": {
      "type": "string",
      "label": "作业名称",
      "default": "torch_ddp",
      "description": "在队列中显示的作业名"
    },
    "partition": {
      "type": "choice",
      "label": "分区",
      "choices": ["P107-RTX5090", "P107-A100"],
      "default": "P107-RTX5090",
      "description": "计算分区"
    },
    "account": {
      "type": "string",
      "label": "计费账户",
      "default": "competition",
      "description": "与分区匹配：P107 系列用 competition，Students 用 stu"
    },
    "qos": {
      "type": "string",
      "label": "QoS",
      "default": "qos_p107-rtx5090",
      "description": "与分区匹配：qos_p107-rtx5090 / qos_p107-a100 / qos_stu_default"
    },
    "gpu_count": {
      "type": "int",
      "label": "GPU 数量",
      "default": 2,
      "min": 2,
      "max": 4,
      "description": "申请的 GPU 卡数（DDP 至少 2 卡）"
    },
    "cpu_count": {
      "type": "int",
      "label": "CPU 核数",
      "default": 8,
      "min": 4,
      "max": 16,
      "description": "申请的 CPU 核数"
    },
    "time_hours": {
      "type": "int",
      "label": "运行时间（小时）",
      "default": 8,
      "min": 1,
      "max": 72,
      "description": "作业最长运行时间"
    },
    "conda_env": {
      "type": "string",
      "label": "conda 环境名",
      "default": "dl",
      "description": "已配置好的 conda 环境名称"
    },
    "train_script": {
      "type": "string",
      "label": "训练脚本路径",
      "default": "train_ddp.py",
      "description": "支持 DDP 的 Python 训练脚本路径"
    },
    "extra_args": {
      "type": "string",
      "label": "额外参数",
      "default": "",
      "description": "传递给训练脚本的额外命令行参数"
    },
    "work_dir": {
      "type": "string",
      "label": "工作目录",
      "default": "$HOME/projects/dl",
      "description": "代码和数据所在目录"
    }
  },
  "script_template": "#!/bin/bash\n#SBATCH --job-name={{ job_name }}\n#SBATCH --partition={{ partition }}\n#SBATCH --account={{ account }}\n#SBATCH --qos={{ qos }}\n#SBATCH --nodes=1\n#SBATCH --ntasks=1\n#SBATCH --gpus={{ gpu_count }}\n#SBATCH --cpus-per-task={{ cpu_count }}\n#SBATCH --time={{ time_hours }}:00:00\n#SBATCH --output=logs/{{ job_name }}_%j.out\n#SBATCH --error=logs/{{ job_name }}_%j.err\n\n# 进入工作目录\ncd {{ work_dir }} || { echo \"目录 {{ work_dir }} 不存在\"; exit 1; }\n\n# 创建日志目录\nmkdir -p logs\n\n# 激活 conda 环境\nsource $(conda info --base)/etc/profile.d/conda.sh\nconda activate {{ conda_env }} || { echo \"conda 环境 {{ conda_env }} 激活失败\"; exit 1; }\n\n# 记录环境信息\necho \"==========================================\"\necho \"作业 ID: $SLURM_JOB_ID\"\necho \"节点: $(hostname)\"\necho \"GPU 数量: {{ gpu_count }}\"\nnvidia-smi --query-gpu=name --format=csv,noheader 2>/dev/null || echo '无 GPU'\necho \"开始时间: $(date)\"\necho \"==========================================\"\n\n# 使用 torchrun 启动 DDP 训练\n# --nproc_per_node 等于 GPU 数量，每个 GPU 跑一个进程\ntorchrun --nproc_per_node={{ gpu_count }} {{ train_script }} {{ extra_args }}\n\necho \"==========================================\"\necho \"结束时间: $(date)\"\necho \"==========================================\"\n"
}